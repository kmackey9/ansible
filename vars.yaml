#This is where our Ansible variables will be stored
---
ansible_port: 22 # SSH Port
ansible_user: ec2-user # EC2 Username
ansible_ssh_private_key_file: ./ansible_key.pem # Path to my private key on this machine